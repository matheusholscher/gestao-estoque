<%= form_with(model: grupo_produto, local: true) do |form| %>
  <% if grupo_produto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(grupo_produto.errors.count, "error") %> prohibited this grupo_produto from being saved:</h2>

      <ul>
      <% grupo_produto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
